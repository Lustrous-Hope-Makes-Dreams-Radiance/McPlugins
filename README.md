# ExpRule

## 📖 插件简介

在服务器的游戏规则 `keep_inventory` 为 `true` 时，仍使玩家掉落经验值并清空等级。

经验的掉落规则与 `keep_inventory` 为 `false` 时一致。

## 🛠 编译环境

* **Minecraft 版本**: 1.21.1
* **Java 版本**: Java 21
* **构建工具**: Maven

## 可用版本

经过测试，插件可以在以下版本的服务端生效：

* Paper 1.21.11
